#include "main.h"
/**
 *_sqrt_recursion - evaluat function sqt
 *@i:integer
 *@n:integer
 *Return:evaluat sqrt
 */
int _sqrt_recursion(int n)
{
if (n == 0 || n == 1)
return (n);
else if (i * i == n)
return (i);
return (-1);
return (-1);
}
